[core]
	autocrlf = input
	eol = lf
[push]
	default = current
	autoSetupRemote = true
[pull]
	ff = only
[merge]
	ff = false
[log]
	decorate = auto
[alias]
	unstage = reset HEAD
[init]
	# "master" in this case is the antonym of copy, not slave.
        defaultBranch = master
